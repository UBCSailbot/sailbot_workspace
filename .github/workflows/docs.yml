name: Docs Site

on:
    push:
      branches:
        - main
      paths:
        - 'docs/**'
    pull_request:
      types: [opened, synchronize, reopened, closed]
      paths:
        - 'docs/**'
    schedule:
      - cron: '0 0 * * 0'  # Runs on main every Sunday at midnight UTC
    workflow_dispatch:

jobs:
  # https://github.com/nosborn/github-action-markdown-cli
  markdownlint:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
    - name: Checkout workspace
      uses: actions/checkout@v7

    - name: Run linter
      uses: nosborn/github-action-markdown-cli@v3.5.0
      with:
        config_file: .markdownlint.json
        files: .
        dot: true

  # https://github.com/gaurav-nelson/github-action-markdown-link-check
  markdown-link-check:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
    - name: Checkout workspace
      uses: actions/checkout@v7

    - name: Run linter
      uses: gaurav-nelson/github-action-markdown-link-check@v1
      with:
        config-file: .markdown-link-check.json
        folder-path: '.'

  markdown-link-redirection-check:
    runs-on: ubuntu-latest
    steps:
      - name: Check out code
        uses: actions/checkout@v7

      - name: Set up Python 3.10
        uses: actions/setup-python@v6
        with:
          python-version: "3.10"

      - name: Check links to external sites redirect to a new tab
        uses: ./.github/actions/docs-lint/
        with:
          folder-path: '.'
        env:
          ROOT: './docs/'

  # https://squidfunk.github.io/mkdocs-material/publishing-your-site/#with-github-actions
  docs-update:
    runs-on: ubuntu-latest
    # Every docs preview and main deployment writes to the same gh-pages branch.
    # Serialize these jobs so one run cannot make another run's checkout stale.
    concurrency:
      group: docs-gh-pages-deployment
      cancel-in-progress: false
    permissions:
      contents: write
    steps:
      - name: Check out code
        uses: actions/checkout@v7
        with:
          # checkout all commits to get accurate page revision times
          # for the git-revision-date-localized plugin
          # and to fetch the gh-pages branch
          fetch-depth: '0'

      - name: Start containers
        shell: bash
        run: docker compose -f .devcontainer/docker-compose.yml -f .devcontainer/docs/docker-compose.docs.yml up -d

      - name: Build docs
        if: github.event_name == 'push' || github.event.action != 'closed'
        shell: bash
        run: docker compose -f .devcontainer/docker-compose.yml -f .devcontainer/docs/docker-compose.docs.yml exec -T docs /bin/sh -c "mkdocs build"

      - name: "Delete pr-${{ github.event.number }} version of the Docs"
        if: github.event_name == 'pull_request' && github.event.action == 'closed'
        shell: bash
        run: |
          docker compose -f .devcontainer/docker-compose.yml -f .devcontainer/docs/docker-compose.docs.yml exec -T docs /bin/sh -c "\
            git config user.name ci-bot && \
            git config user.email ci-bot@example.com && \
            git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} && \
            mike delete --push pr-${{ github.event.number }}"

      - name: "Deploy pr-${{ github.event.number }} version of the Docs"
        if: github.event_name == 'pull_request' && github.event.action != 'closed'
        shell: bash
        run: |
          docker compose -f .devcontainer/docker-compose.yml -f .devcontainer/docs/docker-compose.docs.yml exec -T docs /bin/sh -c "\
            git config user.name ci-bot && \
            git config user.email ci-bot@example.com && \
            git switch gh-pages && \
            git switch - --detach && \
            mike deploy pr-${{ github.event.number }} && \
            git switch gh-pages && \
            git pull --no-rebase && \
            git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} && \
            git push"

      - name: Deploy main version of the Docs
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        shell: bash
        run: |
          docker compose -f .devcontainer/docker-compose.yml -f .devcontainer/docs/docker-compose.docs.yml exec -T docs /bin/sh -c "\
            git config user.name ci-bot && \
            git config user.email ci-bot@example.com && \
            git switch gh-pages && \
            git switch - && \
            mike deploy --update-aliases main latest && \
            git switch gh-pages && \
            git pull --no-rebase && \
            git remote set-url origin https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }} && \
            git push"
