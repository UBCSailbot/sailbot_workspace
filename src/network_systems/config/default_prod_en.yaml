# Configuration that enables the mandatory production modules and uses their defaults
can_transceiver_node:
  ros__parameters:
    enabled: true

local_transceiver_node:
  ros__parameters:
    enabled: true

remote_transceiver_node:
  ros__parameters:
    enabled: true
